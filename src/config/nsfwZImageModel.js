/**
 * Basename of the UNet on the RunPod worker (symlinked from volume `models/unet/` into Comfy).
 * Keep in sync with `runpod-mdcln/workflows/*` UNETLoader / refiner nodes.
 */
export const NSFW_ZIMAGE_UNET_BASENAME = "zImageTurboNSFW_62BF16.safetensors";
